import os
import sys

inputFile = sys.argv[1]
outputFile = sys.argv[2]

def createOrOpen(fileName, mode):
    if os.path.exists(outputFile):
        open(fileName, mode)
        print("Open file str 
            createdFile = open(fileName, 'x')
            createdFile.close()
            print("Create file str(fileName)")
            createOrOpen(fileName, mode)
            
createOrOpen(inputFi
